package com.example.repository;

import org.springframework.stereotype.Repository;

@Repository
public interface TicketRepository extends ITicketRepository {
    // All methods are inherited from ITicketRepository
}